@model Silerium.Models.Subcategory

@{
    ViewData["Title"] = "Удаление подкатегории";
}

<h1>Удаление категории</h1>

<p>
    Вы уверены что хотите удалить подкатегорию? 
    <br />
    Товарам с несуществующими подкатегориями на сайте отображаться не будут.
</p>
<hr />
<div>
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayName("Название")
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayName("Категория")
        </dt>
        <dd class="col-sm-10">
            @if(Model.Category != null)
            {
                @Html.DisplayFor(model => model.Category.Name);
            }
        </dd>
    </dl>
    
    <form asp-action="DeleteCategory">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Удалить" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-secondary">К админ панели</a>
    </form>
</div>
