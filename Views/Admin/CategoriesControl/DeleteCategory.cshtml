@model Silerium.Models.Category

@{
    ViewData["Title"] = "Удаление категории";
    Layout = "_AdminPanelLayout";
}

<h1>Удаление категории</h1>

<p>Вы уверены что хотите удалить категорию? 
    <br />
    С удалением категории также удаляться и подкатегории.
    <br />
    Товарам с несуществующими подкатегориями на сайте отображаться не будут.
</p>
<hr />
<div>
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayName("Название")
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayName("Подкатегории")
        </dt>
        <dd class="col-sm-10">
            @if(Model.Subcategories.Count() != 0)
            {
                foreach(var subCategory in Model.Subcategories)
                {
                    @Html.DisplayFor(item => subCategory.Name);
                }
            }
        </dd>
    </dl>
    
    <form asp-action="DeleteCategory">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Удалить" class="btn btn-danger" />
    </form>
</div>
